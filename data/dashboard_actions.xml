<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ── Backend Client Action – Live Dashboard ────────────────────────── -->
    <record id="action_pos_analytics_dashboard" model="ir.actions.client">
        <field name="name">Live Dashboard</field>
        <field name="tag">pos_analytics_dashboard</field>
        <field name="target">main</field>
    </record>

    <!-- ── Window Action – Report Wizard ─────────────────────────────────── -->
    <record id="action_pos_analytics_report_wizard" model="ir.actions.act_window">
        <field name="name">Analytics Reports</field>
        <field name="res_model">pos.analytics.report.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="context">{}</field>
    </record>

    <!-- ── Window Action – Sales Targets ─────────────────────────────────── -->
    <record id="action_pos_analytics_targets" model="ir.actions.act_window">
        <field name="name">Sales Targets</field>
        <field name="res_model">pos.analytics.target</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{'search_default_active': 1}</field>
    </record>

    <!-- ── Window Action – Configuration (res.config.settings) ───────────── -->
    <record id="action_pos_analytics_config" model="ir.actions.act_window">
        <field name="name">Analytics Configuration</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="pos_advanced_analytics.view_pos_analytics_config_form"/>
        <field name="target">inline</field>
        <field name="context">{'module': 'pos_advanced_analytics'}</field>
    </record>

</odoo>
